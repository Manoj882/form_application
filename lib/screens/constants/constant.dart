import 'package:flutter/material.dart';

const basePadding = EdgeInsets.symmetric(
  vertical: 8.0,
  horizontal: 16.0,
);

class UserConstants{
  static const credentialBox = 'credentails';
  static const usernameKey = 'username';
  static const passwordKey = 'password';

}
